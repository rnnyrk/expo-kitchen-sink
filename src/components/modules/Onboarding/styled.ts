import Animated from 'react-native-reanimated';
import styled from 'styled-components/native';

export const OnboardingBg = styled(Animated.View)`
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: -1;
`;
